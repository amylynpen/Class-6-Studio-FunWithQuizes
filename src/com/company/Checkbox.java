package com.company;

public class Checkbox extends Question{
}

/*
return answersCorrect;
}

@Override
public int getAnswers() {
    ArrayList<Integer> usersAnswers = new ArrayList<>();
    System.out.println("For this question, there are multiple correct answers. To answer the question, enter 'y'.");
    Scanner userContinue = new Scanner(System.in);
    String goToContinue = userContinue.nextLine();
    while (goingToContinue.indexOf('y') >= 0 && userAnswers.size() <= possibleAnswers.size()) {
    System.out.println("What will you answer be? Enter the number of the option you believe is correct.");
    Scanner userAnswer = new Scanner(System.in);
    String answer = userAnswer.nextLine();
    int option = Integer.parseInt(answer);
    usersAnswers.add(option);

    System.out.println("For this question, there are multiple correct answers. To answer the question, enter 'y'.");
    goingToContinue = userContinue.nextLine();
    }
    return checkAnswers(usersAnswers);
    }
    }
 */